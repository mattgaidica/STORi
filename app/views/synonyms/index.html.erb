<p id="notice"><%= notice %></p>

<h1>Synonyms</h1>

<table>
  <thead>
    <tr>
      <th>Keyword 1</th>
      <th>Keyword 2</th>
      <th>Strength</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @synonyms.each do |synonym| %>
      <tr>
        <td><%= synonym.keyword_1 %></td>
        <td><%= synonym.keyword_2 %></td>
        <td><%= synonym.strength %></td>
        <td><%= link_to 'Show', synonym %></td>
        <td><%= link_to 'Edit', edit_synonym_path(synonym) %></td>
        <td><%= link_to 'Destroy', synonym, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Synonym', new_synonym_path %>
